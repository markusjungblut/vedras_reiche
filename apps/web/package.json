{
  "name": "@vedras/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "pretypecheck": "npm run build --workspace @vedras/game-core",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.help-test.json && node --test tests/*.test.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vedras/game-core": "*",
    "@vedras/protocol": "^0.1.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.3.0"
  }
}
