# Exact public browser origin. Do not include a trailing slash or a path.
WEB_ORIGIN=https://staging.example.invalid
PORT=3000
VEDRAS_DATA_DIR=/data
NODE_ENV=production
# Optional public build marker, for example a Git commit SHA from CI.
VEDRAS_BUILD_ID=local
