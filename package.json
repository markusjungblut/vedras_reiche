{
  "name": "vedras-reiche",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "dev:core": "npm run dev --workspace @vedras/game-core",
    "dev:web": "npm run build --workspace @vedras/game-core && npm run build --workspace @vedras/protocol && npm run dev --workspace @vedras/web",
    "dev:server": "npm run build --workspace @vedras/game-core && npm run build --workspace @vedras/protocol && npm run dev --workspace @vedras/server",
    "dev:multiplayer": "concurrently -k \"npm run dev:server\" \"npm run dev:web\"",
    "build": "npm run build --workspace @vedras/game-core && npm run build --workspace @vedras/protocol && npm run build --workspace @vedras/server && npm run build --workspace @vedras/web",
    "typecheck": "npm run build --workspace @vedras/game-core && npm run build --workspace @vedras/protocol && npm run typecheck --workspace @vedras/game-core && npm run typecheck --workspace @vedras/protocol && npm run typecheck --workspace @vedras/server && npm run typecheck --workspace @vedras/web",
    "test": "npm run test --workspace @vedras/game-core && npm run build --workspace @vedras/protocol && npm run test --workspace @vedras/server"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
