{
  "name": "vedras-reiche",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run build --workspace @vedras/game-core && npm run dev --workspace @vedras/web",
    "dev:core": "npm run dev --workspace @vedras/game-core",
    "build": "npm run build --workspace @vedras/game-core && npm run build --workspace @vedras/web",
    "typecheck": "npm run typecheck --workspace @vedras/game-core && npm run typecheck --workspace @vedras/web",
    "test": "npm run test --workspace @vedras/game-core"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
